"""world_state tool package: RunWorldState v0.1 data layer tools.

This package contains validators and the delta applier for the controlled
RunWorldState mechanism. It never reads .env and never calls a real provider.
"""
